@model IEnumerable< ProyectAplication.Models.Star>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name_S)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tipo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdConstellation)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name_S)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tipo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdConstellation)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdCStar }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdCStar }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdCStar })
        </td>
    </tr>
}

</table>
